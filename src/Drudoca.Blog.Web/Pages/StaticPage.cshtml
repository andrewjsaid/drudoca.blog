@page "/s/{uriSegment}"
@model StaticPageModel
@{
    ViewData["Title"] = Model.SPage!.Title;
    ViewData["PageMetadata"] = Model.SPage!.PageMetadata;
}

    <article>
        @Html.Raw(Model.SPage!.Html)
    </article>

